type User {
  id: ID! @id
  email: String! @unique
  password: String!
  role: UserRole!
  profileImage: String
  pokemons: [Pokemon]! @relation(name: "PokemonsOfUser", onDelete: CASCADE)
}

enum UserRole {
  NORMAL
  ADMIN
}
